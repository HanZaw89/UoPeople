# UoPeople

This dashboard is published as a GitHub Pages site. Open the site at the repository Pages URL to view the interactive pathway tracker.

If the site shows a blank page or only a heading, ensure that GitHub Pages is serving the repository root and that the published branch includes both index.html and 404.html.